---
title: "从知道到行动之间的闸门（3/3）"
subtitle: "当本体不只描述现实，还开始约束软件与 AI 如何行动时，它才真正变得重要。"
description: "一篇关于操作型本体、实体解析、受控动作，以及 AI 建议与现实执行之间边界的技术终篇。"
date: 2026-07-11
draft: false
personaKicker: "杂谈 · Palantir 笔记"
url: "/zh/persona/between-knowing-and-doing/"
translationUrl: "/persona/between-knowing-and-doing/"
translationLabel: "English"
translationCta: "Read in English"
series: ["Persona"]
tags: ["Ontology", "AI Systems", "Governance"]
build:
  list: never
  render: always
---

前两篇分别讨论了操作型本体能够带来什么，以及它随之引出的治理问题。这一篇把视线移到机器内部。真正重要的变化，不是模型从小变大，而是软件从描述一个组织，走向在一个被刻意限定的现实模型中行动。

## 本体位于哪一层

数据库首先是存储层；语义层负责稳定指标和定义；知识图谱表达实体与关系；操作型本体则进一步加入状态、可执行动作、权限和审计。

因此，本体并不是一个装着整个组织的神奇文件格式。它是一层叠在普通数据存储之上的定义、服务与代码。以 Foundry 的思路看，这个结构可以拆成四层：

- **存储层：** 标准数据集保存从源系统抽取的记录。
- **定义层：** 对象类型、链接、动作和函数说明这些记录意味着什么。
- **服务层：** 被索引的对象支持快速查询和受控写回。
- **类型化接口：** SDK 把对象与动作暴露给应用，让开发者不必直接面对裸表。

形式本体的传统远早于今天的 AI 平台。RDF、OWL 和 SPARQL 都试图表达共享概念与关系；医疗领域也长期通过 SNOMED CT、ICD、LOINC、RxNorm，以及 OMOP 等通用数据模型做类似的工作。新的操作性变化，是把这些定义进一步连接到动作。

## 动作层

一个动作类型需要说明：它作用于哪个对象，接受哪些输入，允许修改什么，必须满足哪些前置条件，以及谁有权调用或批准它。

动作执行后产生的应当是受控且留痕的修改，而不是一次无声的数据库更新。系统可以记录改了什么、为什么改、由谁批准，以及能否撤回。到这里，本体不再只是一张地图，也成为一条操作边界。

对 AI 而言，这种边界比一句宽泛的“保持安全”更有用。模型可以起草建议，却没有权力直接执行；它可以调用预先定义的动作，却不能临时创造一种新的写入方式；风险更高的动作还可以要求人工批准。

这并不能消除模型错误，但可以缩小错误能够抵达的范围。

## 实体解析必须先发生

对象能够支撑动作之前，系统必须先判断哪些记录指向现实中的同一个对象。不同源系统之间往往没有共享的键。在医疗中，这就是常见的患者匹配或主索引问题。

当可靠标识存在时，可以做确定性匹配；当身份只能从姓名、生日、地址或记录历史等字段组合中推断时，就需要概率匹配。一个典型流程包括：

1. 标准化并清洗源字段。
2. 用分块规则缩小候选比较范围。
3. 对候选匹配进行评分。
4. 自动处理明确案例，把灰区交给人工复核。
5. 当来源冲突时应用幸存者规则。
6. 分配稳定身份，同时保留回到源记录的数据血缘。

不存在完美的阈值。错并会把两个人当成一个，漏并会把一个人拆成两个。不同工作流面对两类错误的代价不同，因此阈值并不只是技术参数。它也编码了组织更愿意承受哪一种错误。

## AI 系统的两条通道

把读取与写入视为两条独立通道之后，这套结构会更容易理解。

**读取通道**检索带有权限和来源血缘的结构化对象。如果相关信息不存在，系统应该暴露这种缺失，而不是补出一个听起来合理的细节。模型仍可能推理错误，但它使用的事实输入可以被检查。

**写入通道**把模型限制在预先定义的动作内。校验、权限、批准规则和审计日志位于模型生成的建议与现实修改之间。模型得到的不是任意读写数据库的权力，而是和系统其他部分一样，通过受治理的动作来操作。

这里的技术承诺很克制，却很重要：不是“模型不会错”，而是“一个错误输出不会自动变成不受约束的现实动作”。

## 一个临床例子

考虑一个看似简单的问题：一位阿尔茨海默病及相关痴呆患者，在 index date 之后 180 天内是否发生过一次急诊就诊？

在普通分析流程中，分析师会用 SQL 或其他转换层定义队列、index date、观察窗、删失规则、就诊编码和去重逻辑。这个结果可以很严谨，但它的含义往往仍藏在某个项目专用的代码里。

RAG 加 SQL 的助手可以把自然语言问题转换成查询，却仍要猜测哪个日期是 index，以及什么算作一次急诊就诊。相似的字段名可能分别表示到达、入院、出院、计费或转院事件。因此，一个语法完全正确的查询，仍可能返回一个含义错误但看起来合理的数字。

在本体化方法中，时间定义可以挂在 `Care Episode` 上并被重复使用。index event、post-index window、censoring event 和 qualifying encounter 都成为明确的对象或属性，而不是每次查询时重新拼出的假设。Agent 可以读取这些受治理的对象，并在合适时创建复核建议；但除非相关动作和审批路径已经被明确建模，它仍不能直接修改 care plan。

真正的分水岭不是自然语言还是 SQL，而是语义和允许的动作是否存在于可复用、可审查的基础设施中。

## MCP 不等于本体

Function calling 解决模型如何调用一个工具；MCP 解决模型如何通过统一协议连接许多工具与资源；本体则回答工具背后的现实意味着什么，以及哪些动作在其中有效。

一种协议可以把模型接到数据库，却不会自动告诉它哪个字段代表临床事件，哪个只是计费产物，哪个 encounter 才建立 index date。连接提供访问，本体提供受治理的解释。

## 成熟度阶梯也是责任阶梯

我们很容易把 AI 系统描述成一条整齐的进阶路线：

- L1：聊天机器人
- L2：检索助手
- L3：工具型 Agent
- L4：带确认环节的工作流 Agent
- L5：本体化操作 Agent

但每向前一步，增加的不只是能力，也是责任。一个系统越从回答走向行动，就越需要稳定定义、狭窄权限、可见的不确定性、复核路径和持久的审计记录。

这也是整个系列的技术结论。本体之所以重要，不是因为它让 AI 听起来知道得更多，而是因为它给了组织一个位置，去决定 AI 可以知道什么、可以建议什么，以及必须在哪里停下。
